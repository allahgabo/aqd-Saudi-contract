import React from 'react';

/**
 * BackendStatus — removed by user request.
 * The app loads normally even if Django is offline;
 * individual pages show empty states gracefully.
 */
export default function BackendStatus() {
  return null;
}

export function BackendErrorCard({ onRetry }: { onRetry?: () => void }) {
  return null;
}
